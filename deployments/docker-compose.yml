version: '3.9'

services:
  anti-brute-force:
    build:
      context: ..
      dockerfile: build/Dockerfile
    environment:
      LISTEN: 8080
    ports:
      - "8080:8080"
    restart: on-failure